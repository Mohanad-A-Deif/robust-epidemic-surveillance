# RKI data pipeline

This directory contains the source preparation, incidence transformation, chronological splitting, fixed-template corruption generation, and validation code.

Run from the repository root through:

```bash
python scripts/reproduce_data.py
```

The canonical configuration is `configs/data_config.json`. The copy in this directory is retained for standalone execution.
