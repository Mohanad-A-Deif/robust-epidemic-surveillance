# Package status

The complete raw source, processed dataset, 20 corruption templates, and 280 scenario files are included at the repository root under `data/`. The pipeline has passed its offline integration test and full-output validation.
